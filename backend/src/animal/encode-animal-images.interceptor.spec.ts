import { EncodeAnimalImagesInterceptor } from './encode-animal-images.interceptor';

describe('EncodeAnimalImagesInterceptor', () => {
  it('should be defined', () => {
    expect(new EncodeAnimalImagesInterceptor()).toBeDefined();
  });
});
